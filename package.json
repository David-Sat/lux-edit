{
  "name": "visual-edit-monorepo",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "lint": "pnpm -r exec tsc --noEmit",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "pnpm lint"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
